{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter '@workspace/jasa-tugas' run build",
    "dev": "pnpm --filter '@workspace/jasa-tugas' run dev",
    "typecheck": "pnpm --filter '@workspace/jasa-tugas' run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
